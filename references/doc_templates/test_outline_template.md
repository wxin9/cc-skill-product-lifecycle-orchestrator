# 主测试大纲（Master Test Outline）

> **版本：** v1.0  |  **日期：** YYYY-MM-DD
> **基于 PRD 版本：** v1.0  |  **架构版本：** v1.0
>
> 文档规范：IEEE 829 精华 + BDD Given/When/Then 语义
> 文档验证：`./lifecycle validate --doc Docs/tests/MASTER_OUTLINE.md --type test_outline`
>
> **此文件是测试追溯的权威来源。** 任何需求或代码变更后，
> 运行 `./lifecycle outline trace --features F01,F02` 找出受影响的测试用例。

---

## 测试覆盖概述

| 功能 ID | 功能名称 | 场景数 | 正常路径 | 异常路径 | E2E |
|---|---|---|---|---|---|
| F01 | （功能名称） | 2 | 1 | 1 | ✓ |
| F02 | （功能名称） | 2 | 1 | 1 | ✓ |
| F03 | （功能名称） | 2 | 1 | 1 | ✓ |

---

## F01 — （功能名称）

**PRD 来源：** `PRD-F01`

### TST-F01-S01 — （正常路径场景名称） `[E2E]` `[UI]`

**前置条件：**
- （系统状态，如「用户已登录」）
- （数据状态，如「已存在至少 3 条记录」）

**测试步骤：**
1. （Given）初始状态描述 — 用户在（哪个页面/入口）
2. （When）用户执行（操作）
3. （When）系统处理（请求）
4. （Then）验证（期望响应）
5. （Then）验证（数据持久化）

**期望结果：** （明确可验证的结果描述，说明什么状态/数据/响应算「通过」）

**测试数据：** （所需的具体数据，如文件路径、参数值、账号信息）

**状态：** `active`

---

### TST-F01-S02 — （异常路径场景名称） `[E2E]` `[UI]`

**前置条件：**
- （触发异常所需的状态，如「使用无效凭证」）

**测试步骤：**
1. （Given）用户在（入口）
2. （When）用户输入（无效/缺失数据）
3. （When）用户触发（操作）
4. （Then）系统返回（错误响应）
5. （Then）验证（错误信息的准确性）

**期望结果：** 系统显示清晰的错误提示；不发生数据损坏；支持用户重试

**测试数据：** （触发异常的具体数据，如超长字符串、空值、越界值）

**状态：** `active`

---

## F02 — （功能名称）

**PRD 来源：** `PRD-F02`

### TST-F02-S01 — （正常路径场景名称） `[E2E]` `[API]`

**前置条件：**
- （依赖哪些前置功能，如「已完成 F01」）

**测试步骤：**
1. （Given）（初始状态）
2. （When）（触发操作，如「调用 POST /api/v1/...」）
3. （When）（系统异步处理，如「等待 Celery 任务完成」）
4. （Then）验证（响应格式和内容）
5. （Then）验证（副作用，如数据库记录更新）

**期望结果：** （具体结果描述）

**测试数据：** （请求体、文件、环境参数）

**状态：** `active`

---

### TST-F02-S02 — （异常路径场景名称） `[E2E]` `[API]`

**前置条件：**
- （触发异常的条件）

**测试步骤：**
1. （Given）（前置状态）
2. （When）发送（无效/缺失字段的请求）
3. （Then）验证返回 4xx 错误码
4. （Then）验证错误信息包含（具体字段提示）

**期望结果：** 返回适当的错误响应；不产生部分写入的脏数据

**测试数据：** （触发错误的具体输入）

**状态：** `active`

---

## F03 — （功能名称）

**PRD 来源：** `PRD-F03`

### TST-F03-S01 — （正常路径） `[E2E]` `[UI]`

**前置条件：**
- （前置条件）

**测试步骤：**
1. （步骤）
2. （步骤）
3. （步骤）

**期望结果：** （期望结果）

**测试数据：** （测试数据）

**状态：** `active`

---

### TST-F03-S02 — （异常路径） `[E2E]` `[UI]`

**前置条件：**
- （前置条件）

**测试步骤：**
1. （步骤）
2. （步骤）
3. （步骤）

**期望结果：** （期望结果）

**测试数据：** （测试数据）

**状态：** `active`
